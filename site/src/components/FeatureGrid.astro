---
import { features } from "../data/content.ts";

const icons: Record<string, string> = {
  compass: '<circle cx="12" cy="12" r="9"/><polygon points="16 8 10 10 8 16 14 14 16 8"/>',
  globe: '<circle cx="12" cy="12" r="9"/><path d="M3 12h18M12 3a15 15 0 0 1 0 18M12 3a15 15 0 0 0 0 18"/>',
  flame: '<path d="M12 3c2 4 5 5 5 9a5 5 0 0 1-10 0c0-2 1-3 2-4 0 2 1 3 2 3 0-3-1-5-1-8z"/>',
  route: '<circle cx="6" cy="19" r="2"/><circle cx="18" cy="5" r="2"/><path d="M8 19h6a4 4 0 0 0 0-8H10a4 4 0 0 1 0-8h6"/>',
  lock: '<rect x="5" y="11" width="14" height="9" rx="2"/><path d="M8 11V7a4 4 0 0 1 8 0v4"/>',
  activity: '<polyline points="3 12 8 12 11 20 14 4 17 12 21 12"/>',
  shield: '<path d="M12 3l8 3v6c0 5-4 8-8 9-4-1-8-4-8-9V6z"/>',
  settings: '<circle cx="12" cy="12" r="3"/><path d="M12 2v3M12 19v3M4 12H1M23 12h-3M5 5l2 2M17 17l2 2M5 19l2-2M17 7l2-2"/>',
  puzzle: '<path d="M10 3h4v3a2 2 0 0 0 4 0V3h3v18H4V3h3v3a2 2 0 0 0 4 0z"/>',
  layers: '<polygon points="12 3 21 8 12 13 3 8 12 3"/><polyline points="3 13 12 18 21 13"/>',
};
---
<section class="section" id="features">
  <div class="container">
    <h2 class="h2 head" data-reveal>Everything you need to operate RouterOS with AI</h2>
    <div class="grid">
      {features.map((f) => (
        <article class="card" data-reveal>
          <span class="ico" aria-hidden="true">
            <svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="1.6" stroke-linecap="round" stroke-linejoin="round" set:html={icons[f.icon]} />
          </span>
          <h3>{f.title}</h3>
          <p>{f.body}</p>
        </article>
      ))}
    </div>
  </div>
</section>

<style>
  .head { max-width: 620px; margin-bottom: 40px; }
  .grid { display: grid; grid-template-columns: repeat(3, 1fr); gap: 1px; background: var(--border); border: 1px solid var(--border); border-radius: var(--radius); overflow: hidden; }
  .card {
    background: var(--bg); padding: 26px 24px; transition: background .18s ease;
    position: relative;
  }
  .card:hover { background: var(--bg-elev); }
  .ico {
    display: inline-flex; align-items: center; justify-content: center;
    width: 34px; height: 34px; border-radius: 8px; color: var(--brand-ink);
    background: rgba(255,255,255,.03); border: 1px solid var(--border-strong); margin-bottom: 16px;
  }
  .ico svg { width: 18px; height: 18px; }
  .card h3 { font-size: 16px; margin-bottom: 8px; }
  .card p { margin: 0; color: var(--text-dim); font-size: 14px; line-height: 1.65; }

  @media (max-width: 900px) { .grid { grid-template-columns: repeat(2, 1fr); } }
  @media (max-width: 600px) { .grid { grid-template-columns: 1fr; } }
</style>
