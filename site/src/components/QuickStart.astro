---
const tabs = [
  { id: "npm", label: "npm", lines: ["npm install -g mikromcp", "mikromcp init"] },
  { id: "binary", label: "Binary", lines: ["# Download from GitHub releases, then", "chmod +x mikromcp-linux-x64", "./mikromcp-linux-x64 init"] },
  { id: "docker", label: "Docker", lines: ["docker pull ghcr.io/alikarami/mikromcp:latest"] },
];
---
<section class="section qs" id="quick-start">
  <div class="container">
    <h2 class="h2 head" data-reveal>Up and running in minutes</h2>

    <div class="tabs" data-reveal>
      <div class="tablist" role="tablist" aria-label="Install methods">
        {tabs.map((t, i) => (
          <button class={`tab mono ${i === 0 ? "active" : ""}`} role="tab" id={`qs-tab-${i}`}
            aria-selected={i === 0 ? "true" : "false"} aria-controls={`qs-panel-${i}`}
            tabindex={i === 0 ? "0" : "-1"} data-i={i}>{t.label}</button>
        ))}
      </div>

      {tabs.map((t, i) => (
        <div class={`panel ${i === 0 ? "active" : ""}`} role="tabpanel" id={`qs-panel-${i}`}
          aria-labelledby={`qs-tab-${i}`} hidden={i !== 0}>
          <div class="code">
            <button class="copy mono" data-copy={t.lines.join("\n")} aria-label={`Copy ${t.label} install command`}>Copy</button>
            <pre class="mono">{t.lines.map((l, j) => (
              <div class={l.startsWith("#") ? "cl comment" : "cl"}>{!l.startsWith("#") && <span class="p" aria-hidden="true">$</span>}<span>{l}</span></div>
            ))}</pre>
          </div>
        </div>
      ))}
    </div>

    <p class="note" data-reveal>
      <code class="mono">mikromcp init</code> runs a guided wizard, then open your AI assistant and just ask.
    </p>
    <p class="docs" data-reveal>
      <a href="https://github.com/AliKarami/MikroMCP/wiki/Getting-Started" target="_blank" rel="noopener">Full Getting Started guide →</a>
    </p>
  </div>
</section>

<style>
  .qs { background: linear-gradient(180deg, rgba(20,184,166,.03), transparent 40%); }
  .head { margin-bottom: 36px; }
  .tabs { max-width: 720px; }
  .tablist { display: flex; gap: 8px; overflow-x: auto; margin-bottom: 16px; padding-bottom: 4px; }
  .tab {
    flex: 0 0 auto; font-size: 13px; padding: 9px 18px; border-radius: 999px; cursor: pointer;
    background: var(--bg-elev); border: 1px solid var(--border); color: var(--text-dim);
    transition: all .15s ease; white-space: nowrap;
  }
  .tab:hover { color: var(--text); border-color: var(--brand-deep); }
  .tab.active { color: #04110F; background: linear-gradient(135deg, var(--brand-glow), var(--brand)); border-color: transparent; font-weight: 500; }
  .tab:focus-visible { outline: 2px solid var(--brand-glow); outline-offset: 2px; }

  .panel { display: none; } .panel.active { display: block; }
  .code { position: relative; background: #0E1318; border: 1px solid var(--border); border-radius: 12px; overflow: hidden; }
  .code pre { margin: 0; padding: 20px 22px; font-size: 13.5px; line-height: 1.9; overflow-x: auto; }
  .cl { color: #D4DEE6; white-space: pre; }
  .cl.comment { color: #5d6b76; }
  .p { color: var(--brand); margin-right: 10px; }
  .copy {
    position: absolute; top: 10px; right: 10px; z-index: 2; font-size: 12px; padding: 6px 12px;
    border-radius: 8px; cursor: pointer; background: rgba(255,255,255,.05);
    border: 1px solid var(--border); color: var(--text-dim); transition: all .15s ease;
  }
  .copy:hover { color: var(--text); border-color: var(--brand); }
  .copy.done { color: #04110F; background: var(--brand-glow); border-color: transparent; }
  .copy:focus-visible { outline: 2px solid var(--brand-glow); outline-offset: 2px; }

  .note { margin: 26px 0 4px; color: var(--text-dim); font-size: 15px; }
  .note code { color: var(--brand-glow); background: rgba(20,184,166,.08); padding: 2px 7px; border-radius: 6px; font-size: 13px; }
  .docs a { color: var(--brand-glow); font-weight: 600; font-size: 14px; }
  .docs a:hover { text-decoration: underline; }
  .docs a:focus-visible { outline: 2px solid var(--brand-glow); outline-offset: 3px; border-radius: 4px; }
</style>

<script>
  document.querySelectorAll<HTMLElement>(".qs .tabs").forEach((root) => {
    const tabs = Array.from(root.querySelectorAll<HTMLButtonElement>(".tab"));
    const panels = Array.from(root.querySelectorAll<HTMLElement>(".panel"));
    const select = (i: number) => {
      tabs.forEach((t, j) => {
        const on = j === i;
        t.classList.toggle("active", on);
        t.setAttribute("aria-selected", String(on));
        t.tabIndex = on ? 0 : -1;
      });
      panels.forEach((p, j) => { p.classList.toggle("active", j === i); p.hidden = j !== i; });
    };
    tabs.forEach((t, i) => {
      t.addEventListener("click", () => select(i));
      t.addEventListener("keydown", (e) => {
        if (e.key === "ArrowRight" || e.key === "ArrowLeft") {
          e.preventDefault();
          const n = (i + (e.key === "ArrowRight" ? 1 : tabs.length - 1)) % tabs.length;
          select(n); tabs[n].focus();
        }
      });
    });
  });

  document.querySelectorAll<HTMLButtonElement>(".qs .copy").forEach((btn) => {
    btn.addEventListener("click", async () => {
      try {
        await navigator.clipboard.writeText(btn.dataset.copy ?? "");
        const prev = btn.textContent;
        btn.textContent = "Copied"; btn.classList.add("done");
        setTimeout(() => { btn.textContent = prev; btn.classList.remove("done"); }, 1500);
      } catch {}
    });
  });
</script>
